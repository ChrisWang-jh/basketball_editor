[tool.ruff]
target-version = "py311"
line-length = 88
cache-dir = "outputs/.cache/ruff"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.pytest.ini_options]
testpaths = ["tests"]
cache_dir = "outputs/.cache/pytest"
